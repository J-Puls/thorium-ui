import { colors } from './colors';

export const linkThemes = {
  dark: {
    boxShadow: `0 2px 0 0`,
    color: colors.dark.primary
  },
  light: {
    boxShadow: `0 2px 0 0`,
    color: colors.light.primary
  }
};
