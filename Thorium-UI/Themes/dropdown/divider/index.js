import { colors } from "../../colors";

export const dividerThemes = {
  dark: {
    color: colors.dark.primary,
    fontWeight: 700,
  },
  light: {
    color: colors.light.primary,
    fontWeight: 700,
  },
};
