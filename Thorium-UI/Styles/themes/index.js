import { button } from "./button-themes";
import { body } from "./body-themes";

// Centralizes all component theme styling 
export const themes = {
  dark: {
    body: body.dark,
    button: button.dark
  },
  light: {
    body: body.light,
    button: button.light
  }
};
